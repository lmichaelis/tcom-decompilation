const int SPL_COST_ACID = 25;
const int SPL_DAMAGE_ACID = 90;
instance SPELL_ACID(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_ACID;
    DAMAGETYPE = DAM_MAGIC;
}

func int SPELL_LOGIC_ACID(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_ACID)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

