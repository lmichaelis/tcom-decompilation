const int SPL_COST_ENERGYBALL = 50;
const int SPL_DAMAGE_ENERGYBALL = 90;
instance SPELL_ENERGYBALL(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_ENERGYBALL;
    DAMAGETYPE = DAM_MAGIC;
}

func int SPELL_LOGIC_ENERGYBALL(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_ENERGYBALL)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

