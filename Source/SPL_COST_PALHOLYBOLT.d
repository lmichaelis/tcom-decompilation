const int SPL_COST_PALHOLYBOLT = 10;
const int SPL_DAMAGE_PALHOLYBOLT = 150;
instance SPELL_PALHOLYBOLT(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_PALHOLYBOLT;
}

func int SPELL_LOGIC_PALHOLYBOLT(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_PALHOLYBOLT)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

