const int SPL_COST_ICECUBE = 40;
const int SPL_FREEZE_DAMAGE = 2;
const int SPL_TIME_FREEZE = 10;
instance SPELL_ICECUBE(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = 60;
}

func int SPELL_LOGIC_ICECUBE(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL2))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_ICECUBE)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

