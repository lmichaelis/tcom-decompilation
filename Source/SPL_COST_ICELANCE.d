const int SPL_COST_ICELANCE = 15;
const int SPL_DAMAGE_ICELANCE = 100;
instance SPELL_ICELANCE(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_ICELANCE;
    DAMAGETYPE = DAM_MAGIC;
}

func int SPELL_LOGIC_ICELANCE(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL2))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_ICELANCE)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

