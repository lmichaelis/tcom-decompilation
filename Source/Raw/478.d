const int SPL_COST_DESTROYUNDEAD = 100;
const int SPL_DAMAGE_DESTROYUNDEAD = 1000;
instance SPELL_DESTROYUNDEAD(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_DESTROYUNDEAD;
    SPELLTYPE = SPELL_NEUTRAL;
}

func int SPELL_LOGIC_DESTROYUNDEAD(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_DESTROYUNDEAD)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

func void SPELL_CAST_DESTROYUNDEAD() {
    if (NPC_GETACTIVESPELLISSCROLL(SELF)) {
        SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_SCROLL);
    };
    SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_DESTROYUNDEAD);
    SELF.AIVAR[20] += 1;
}

