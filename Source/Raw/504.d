const int SPL_COST_SUCKENERGY = 30;
const int SPL_SUCKENERGY_DAMAGE = 100;
const int SPL_TIME_SUCKENERGY = 9;
instance SPELL_SUCKENERGY(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 50;
    TARGETCOLLECTALGO = TARGET_COLLECT_FOCUS;
    TARGETCOLLECTRANGE = 1000;
}

func int SPELL_LOGIC_SUCKENERGY(var int MANAINVESTED) {
    if ((MANAINVESTED) == (0)) {
        return SPL_RECEIVEINVEST;
    };
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SUCKENERGY)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

func void SPELL_CAST_SUCKENERGY() {
    if (NPC_GETACTIVESPELLISSCROLL(SELF)) {
        SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_SCROLL);
    };
    SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_SUCKENERGY);
    SELF.AIVAR[20] += 1;
}

