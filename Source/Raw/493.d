const int SPL_COST_PALLIGHT = 10;
const int SPL_COST_LIGHT = 10;
const int SPL_DURATION_PALLIGHT = 5;
const int SPL_DURATION_LIGHT = 5;
instance SPELL_LIGHT(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 500;
    SPELLTYPE = SPELL_NEUTRAL;
    TARGETCOLLECTALGO = TARGET_COLLECT_NONE;
    TARGETCOLLECTRANGE = 0;
    TARGETCOLLECTAZI = 0;
    TARGETCOLLECTELEV = 0;
}

instance SPELL_PALLIGHT(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 500;
    SPELLTYPE = SPELL_NEUTRAL;
    TARGETCOLLECTALGO = TARGET_COLLECT_NONE;
    TARGETCOLLECTRANGE = 0;
    TARGETCOLLECTAZI = 0;
    TARGETCOLLECTELEV = 0;
}

func int SPELL_LOGIC_PALLIGHT(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_PALLIGHT)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

func void SPELL_CAST_PALLIGHT() {
    if (NPC_GETACTIVESPELLISSCROLL(SELF)) {
        SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_SCROLL);
    };
    SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_PALLIGHT);
    SELF.AIVAR[20] += 1;
}

func int SPELL_LOGIC_LIGHT(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_LIGHT)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

func void SPELL_CAST_LIGHT() {
    if (NPC_GETACTIVESPELLISSCROLL(SELF)) {
        SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_SCROLL);
    };
    SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_LIGHT);
    SELF.AIVAR[20] += 1;
}

