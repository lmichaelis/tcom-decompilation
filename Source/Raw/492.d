const int SPL_COST_INSTANTFIREBALL = 15;
const int SPL_DAMAGE_INSTANTFIREBALL = 75;
instance SPELL_INSTANTFIREBALL(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_INSTANTFIREBALL;
    DAMAGETYPE = DAM_MAGIC;
}

func int SPELL_LOGIC_INSTANTFIREBALL(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_INSTANTFIREBALL)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

func void SPELL_CAST_INSTANTFIREBALL() {
    if (NPC_GETACTIVESPELLISSCROLL(SELF)) {
        SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_SCROLL);
    };
    SELF.ATTRIBUTE[2] = (SELF.ATTRIBUTE[2]) - (SPL_COST_INSTANTFIREBALL);
    SELF.AIVAR[20] += 1;
}

