const int SPL_COST_WHIRLWIND = 20;
const int SPL_WHIRLWIND_DAMAGE = 0;
const int SPL_TIME_WHIRLWIND = 10;
instance SPELL_WHIRLWIND(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    TARGETCOLLECTALGO = TARGET_COLLECT_FOCUS;
    DAMAGE_PER_LEVEL = 60;
}

func int SPELL_LOGIC_WHIRLWIND(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_WHIRLWIND)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

