func void SECRET_REMOVEVRAZKA() {
    FF_APPLYONCEEXTGT(61495, 0, -(1));
}

func void SECRET_REMOVEVRAZKA_APPLY() {
    var ZCMOVER MOVER1;
    var int MOVPTR1;
    var int CHANGEVRAZKACOLLISION;
    if ((CHANGEVRAZKACOLLISION) == (0)) {
        PRINTD("Rozpoczynam - Secret_RemoveVrazka_Apply");
        MOVPTR1 = MEM_SEARCHVOBBYNAME("KM_CORPSE_VRAZKA");
        MOVER1 = MEM_PTRTOINST(MOVPTR1);
        CHANGEVOBCOLLISION("KM_VRAZKA", FALSE, FALSE, FALSE, TRUE);
        WLD_SENDTRIGGER("KM_CORPSE_VRAZKA");
        CHANGEVRAZKACOLLISION = 1;
    };
    if (((MOVER1.MOVERSTATE) != (MOVER_STATE_OPENING)) && ((MOVER1.MOVERSTATE) != (MOVER_STATE_CLOSING))) {
        PRINTD("Skoñczy³em - Secret_RemoveVrazka_Apply");
        CHANGEVOBCOLLISION("KM_VRAZKA", TRUE, TRUE, TRUE, TRUE);
        FF_REMOVE(61495);
        CHANGEVRAZKACOLLISION = 0;
    };
}

