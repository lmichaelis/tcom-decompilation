const int SPL_COST_THUNDERSTORM = 140;
const int SPL_DAMAGE_THUNDERSTORM = 400;
instance SPELL_THUNDERSTORM(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_THUNDERSTORM;
    DAMAGETYPE = DAM_MAGIC;
    TARGETCOLLECTALGO = TARGET_COLLECT_NONE;
}

func int SPELL_LOGIC_THUNDERSTORM(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL4))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_THUNDERSTORM)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

