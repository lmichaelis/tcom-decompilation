const int SPL_COST_WATERFIST = 30;
const int SPL_DAMAGE_WATERFIST = 250;
instance SPELL_WATERFIST(C_SPELL_PROTO) {
    C_SPELL_PROTO();
    TIME_PER_MANA = 0;
    DAMAGE_PER_LEVEL = SPL_DAMAGE_WATERFIST;
    DAMAGETYPE = DAM_MAGIC;
}

func int SPELL_LOGIC_WATERFIST(var int MANAINVESTED) {
    if ((NPC_GETACTIVESPELLISSCROLL(SELF)) && ((SELF.ATTRIBUTE[2]) >= (SPL_COST_SCROLL3))) {
        return SPL_SENDCAST;
    };
    if ((SELF.ATTRIBUTE[2]) >= (SPL_COST_WATERFIST)) {
        return SPL_SENDCAST;
    };
    return SPL_SENDSTOP;
}

